import numpy
import scipy
import matplotlib
import pandas
import nibabel

#import blah    #won't work

#this line makes it a module, without it this file is a script
#the difference is that you can import a module into another file (script or module)
#but you can only run a script directly
if __name__ == '__main__':

    x = 1 + 1
    print(x)
    print('hello world.')